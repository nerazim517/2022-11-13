using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EventSystem_ : MonoBehaviour
{
    public GameObject Monster_Information_UI;
    // Start is called before the first frame update
    void Start()
    {
        Monster_Information_UI.SetActive(false);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
